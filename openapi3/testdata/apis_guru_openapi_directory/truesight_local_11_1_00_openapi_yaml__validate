invalid components: schema "ReinitializeActionConfiguration": invalid example: value is not one of the allowed values [0,1]
Schema:
  {
    "description": "When set to \u003cem\u003e1\u003c/em\u003e, removes all manually set Alert Actions and reverts to basic default actions i.e. trigger a PATROL event and annotate a parameter graph.",
    "enum": [
      0,
      1
    ],
    "example": 1,
    "format": "int32",
    "type": "integer"
  }

Value:
  1
