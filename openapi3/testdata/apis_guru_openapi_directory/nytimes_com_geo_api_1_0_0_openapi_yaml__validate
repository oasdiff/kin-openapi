invalid paths: invalid path /query.json: invalid operation GET: parameter "facets" schema is invalid: invalid default: value is not one of the allowed values [0,1]
Schema:
  {
    "default": 0,
    "enum": [
      0,
      1
    ],
    "type": "integer"
  }

Value:
  0
