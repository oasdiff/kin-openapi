invalid components: schema "Account": invalid example: value is not one of the allowed values [1,0]
Schema:
  {
    "description": "Account status flag. A one (1) means the account is active; zero (0) means it is suspended.",
    "enum": [
      1,
      0
    ],
    "example": 1,
    "format": "int32",
    "type": "integer"
  }

Value:
  1
