invalid components: parameter "search_pattern": parameter "search_pattern" schema is invalid: invalid default: value is not one of the allowed values [0,1,2]
Schema:
  {
    "default": 0,
    "enum": [
      0,
      1,
      2
    ],
    "type": "integer"
  }

Value:
  0
